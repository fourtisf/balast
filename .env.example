# Which DataProvider the app reads from.
#   sim   — P0 simulator, reproduces the prototype's generated pools, vaults and positions
#   live  — P1 indexer-backed provider (not implemented yet; throws on load)
DATA_SOURCE=sim
